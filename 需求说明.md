# 应用需求说明

## 主要目标

为个人用户提供一个极简、现代、卡片式的待办事项管理网页，无需登录、无需后端，数据保存在浏览器本地，打开即用。

## 核心功能

1. **添加待办**：顶部输入框 + 添加按钮，支持回车键提交；内容去除首尾空格，空内容不提交；单条上限 200 字符。
2. **完成 / 取消完成**：每条待办左侧有圆形复选框，点击切换状态；完成后任务文字显示删除线、整体变浅绿底、状态标签变为绿色「已完成」。
3. **删除待办**：每条待办右侧有删除按钮（桌面端悬停显示，移动端常显），点击带滑出动画后删除。
4. **状态展示**：每条待办显示任务名称 + 状态标签（进行中 / 已完成）。
5. **清除已完成**：存在已完成项时，底部显示「清除已完成」按钮，一键批量删除。
6. **统计信息**：顶部副标题动态显示剩余待办数；底部显示总数与已完成数。
7. **空状态**：无待办时显示引导性空状态插画与文案。
8. **本地持久化**：所有数据存入 `localStorage`（键名 `todo_list_items_v1`），刷新 / 关闭后不丢失；读取失败时安全降级为空列表。

## 业务规则 / 逻辑

- 排序规则：未完成项在前，已完成项在后；同组内按创建时间倒序（新的在上）。
- 数据结构：`{ id: string, text: string, completed: boolean, createdAt: number }`。
- ID 生成：时间戳 base36 + 随机串，保证唯一。
- 写入失败（如隐私模式 / 存储满）仅在控制台告警，不阻断 UI。

## 设计与交互约束

- 浅色极简风格，卡片式布局，最大宽度 560px 居中。
- CSS 变量设计系统，主色 `#4f6ef7`，成功色 `#10b981`。
- Inter 字体（中文回退到 PingFang SC / 微软雅黑）。
- 圆角 12–16px，柔和阴影，所有交互元素 0.2s 过渡。
- 响应式断点 480px：移动端内边距收窄、添加按钮收缩为纯图标、删除按钮常显、底部统计纵向排列。
- 无障碍：语义化 HTML、`aria-label`、键盘焦点可见、支持 `prefers-reduced-motion`。

## 补充说明

- 零依赖、零构建，双击 `index.html` 即可运行。
- 不引入任何第三方 JS 库；图标使用内联 SVG。
- 兼容 Chrome / Edge / Safari / Firefox 最新版本。
